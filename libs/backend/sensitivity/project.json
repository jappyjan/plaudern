{
  "name": "sensitivity",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/backend/sensitivity/src",
  "projectType": "library",
  "tags": ["scope:backend", "type:feature"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/backend/sensitivity/tsconfig.json --noEmit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config libs/backend/sensitivity/jest.config.js"
      }
    }
  }
}
