fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
numpy>=1.26
# ponytail: pyannote.audio 3.x checkpoints need torch<2.6 (torch 2.6 defaults
# torch.load to weights_only=True and the checkpoints trip it) and torchaudio<2.9
# (AudioMetaData removed). Keep the stack <2.6 until we move to pyannote.audio >=4.
torch>=2.2,<2.6
torchaudio>=2.2,<2.6
# ponytail: pyannote 3.x + our main.py pass use_auth_token, removed in huggingface_hub 1.0
huggingface_hub>=0.24,<1.0
pyannote.audio>=3.1,<4
faster-whisper>=1.1
