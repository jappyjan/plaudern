fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
numpy>=1.26
# ponytail: pyannote.audio 3.x needs torchaudio.AudioMetaData, removed in torchaudio 2.9 —
# keep the torch stack <2.9 until we move to pyannote.audio >=4
torch>=2.2,<2.9
torchaudio>=2.2,<2.9
pyannote.audio>=3.1,<4
faster-whisper>=1.1
